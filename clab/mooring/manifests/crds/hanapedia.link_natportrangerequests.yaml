---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: natportrangerequests.hanapedia.link
spec:
  group: hanapedia.link
  names:
    kind: NATPortRangeRequest
    listKind: NATPortRangeRequestList
    plural: natportrangerequests
    singular: natportrangerequest
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              natConfig:
                type: string
              nodeName:
                type: string
              podIP:
                type: string
              podName:
                type: string
              podNamespace:
                type: string
              portRangeCount:
                description: |-
                  PortRangeCount is the number of fixed-size blocks to allocate per external IP.
                  Defaults to 1 if unset.
                format: int32
                type: integer
            required:
            - natConfig
            - nodeName
            - podIP
            - podName
            - podNamespace
            type: object
          status:
            properties:
              deletionGracePeriodExpiry:
                description: |-
                  DeletionGracePeriodExpiry is set by the daemon when the pod is deleted.
                  The daemon waits until this time before removing the NATPortRangeRequest.
                format: date-time
                type: string
            type: object
        type: object
    selectableFields:
    - jsonPath: .spec.nodeName
    served: true
    storage: true
    subresources:
      status: {}
